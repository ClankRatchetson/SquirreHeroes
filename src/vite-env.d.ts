/// <reference types="vite/client" />

/** Injecté par `vite.config.ts` (`define`) depuis `package.json` — jamais un appel réseau, juste un remplacement à la compilation. */
declare const __APP_VERSION__: string;
