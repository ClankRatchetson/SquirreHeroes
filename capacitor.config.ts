import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "fr.rivet.squirrelheroes",
  appName: "Squirrel Heroes",
  webDir: "dist",
  backgroundColor: "#1c1917",
};

export default config;
